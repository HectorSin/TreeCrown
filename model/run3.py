import os
from pic_crop import crop, combine

# input_image에 있는 이미지를 400x400 크기로 잘라서 output_image에 저장
# output_image에 있는 400x400 이미지를 다시 붙여 combine_image에 저장

if __name__ == "__main__":
    combine()